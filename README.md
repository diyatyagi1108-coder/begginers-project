# begginers-project
Developed a basic rule-based chatbot using Java Swing and OOP concepts. The chatbot provides automated responses to user inputs through a graphical user interface. Implemented event handling, conditional logic, and GUI components like JFrame, JTextArea, JTextField, and JButton to create an interactive desktop application
public class Main {

    public static void main(String[] args) {

        // Start Login System
        new LoginSystem();
    }
}
